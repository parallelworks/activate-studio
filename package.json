{
  "name": "ade-studio",
  "private": true,
  "version": "0.1.0",
  "description": "Knowledge base visualization and chat/code harness over PW_KNOWLEDGE_BASE, served as an ACTIVATE session",
  "scripts": {
    "dev": "pnpm --parallel --filter @ade-studio/server --filter @ade-studio/web dev",
    "build": "pnpm --filter @ade-studio/web build && pnpm --filter @ade-studio/server build",
    "start": "node server/dist/main.js"
  }
}
